# MongoDB Configuration
# Set MONGODB_USE_LOCAL=true to use local MongoDB instead of Atlas

# Use local MongoDB (localhost:27017)
MONGODB_USE_LOCAL=true
MONGODB_LOCAL_HOST=localhost
MONGODB_LOCAL_PORT=27017
MONGODB_DATABASE=project10

# MongoDB Atlas Configuration (unused if MONGODB_USE_LOCAL=true)
# MONGODB_CONNECTION_STRING=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGODB_USERNAME=username
# MONGODB_PASSWORD=password
# MONGODB_CLUSTER=cluster.mongodb.net
